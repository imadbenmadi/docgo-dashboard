# Local development only. Vite loads this on top of .env when running `dev`,
# so the checked-in production URL in .env stays correct for builds.
#
# http, not https: the dev API server has no certificate. Pointing at https
# here is what produced ERR_SSL_PROTOCOL_ERROR on every request.
VITE_API_URL=http://localhost:3000
